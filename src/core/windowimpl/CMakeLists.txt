if(RAYGAME_GUI_USE_WAYLAND)
    target_sources(${PROJECT_NAME} PUBLIC wayland.cpp wayland.h)
    add_subdirectory(wayland)
    include(FindWayland)
    add_wayland(${PROJECT_NAME} ${RAYGAME_ETC}/wayland-sources)
endif()
